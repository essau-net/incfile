const clickAddTaskButton = require('./click-add-task');
const fillTaskForm = require('./fill-task-form');
const submitCredentials = require('./submit-credentials');
const submitTask = require('./submit-task');
const typeCredentials = require('./type-credentials');

module.exports = {
  clickAddTaskButton,
  fillTaskForm,
  submitCredentials,
  submitTask,
  typeCredentials,
};
